<!-- Delete each comment as you answer it. A leftover comment fails the check. -->

## What changed

<!-- What was wrong or missing, and what this does about it. Evidence beats
     description — paste the real output if you have it.

     Rename this heading after the finding if that reads better. The check
     only asks that some section other than Verification explains the change;
     it does not require these particular words. -->

## Verification

<!-- Required on every pull request, including docs-only ones, where
     "nothing to run" is a complete answer.

     - test counts, and whether any new test was verified to discriminate by
       reverting the fix and watching it fail
     - `pytest evals/` if this touches chunk.py, store.py, retrieve.py,
       generate.py or config.py. CI never runs evals, so this is the only
       place that result can exist. "Not applicable, because ..." is fine. -->
